---
"@fentaris/cli": patch
---

Show compact doctor and check output with a summary line and issues only by default, add `--verbose`, and check only the configured package manager instead of every supported manager.
