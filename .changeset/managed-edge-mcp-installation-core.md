---
"@fentaris/core": minor
---

Add declarative managed installation for edge MCP deployments: `edge.npm(...)` install plans on `stdio(...)`, install plans covered by the launch-recipe digest, cloud-target rejection, the `install-required` deployment readiness status with bounded install reason categories, and an optional install report on the edge setup-status message.
