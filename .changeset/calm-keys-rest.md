---
"@fentaris/cli": patch
---

Generate a project-local encryption key on the first credential write, load project `.env` files from generated runtime scripts, and allow read-only secret commands when no encrypted store exists.
