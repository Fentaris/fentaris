---
"@fentaris/cli": minor
---

Allow `fentaris secrets` commands to run in SDK-only projects by discovering `package.json` projects that depend on `@fentaris/core`, including optional `package.json` metadata and `--entrypoint` support for manifest generation.
