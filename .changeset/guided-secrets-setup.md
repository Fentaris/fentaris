---
"@fentaris/core": major
"@fentaris/cli": minor
---

Validate every declared runtime credential before opening a transport, and add guided, idempotent project credential and API-key setup through `fentaris secrets setup`.
