---
"@fentaris/edge": minor
---

Install the MCP software a deployment pins: `ManagedInstallManager` installs allowlisted packages with lifecycle scripts disabled and a bounded timeout, verifies version, bin containment, and integrity before promoting an install, launches the declared bin from the managed directory instead of the device `PATH`, reuses installs across deployments and restarts, and prunes installs no desired deployment references.
